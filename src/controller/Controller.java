package controller;

/**
 * The {@code Controller} interface defines
 * the primary control flow method for the application.
 */
public interface Controller {
  /**
   * Starts the application control flow,
   * handling user inputs and model updates accordingly.
   */
  void toGo();
}
